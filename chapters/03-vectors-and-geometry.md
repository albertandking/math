# 第 3 章 向量与几何直觉

## 本章为什么重要

在前两章里，我们主要讨论的是“一个输入如何影响一个输出”。但真实的机器学习问题里，输入往往不只是一个数字，而是一组数字。

例如，预测房价时，一个样本可能同时包含：

- 面积
- 楼层
- 距离地铁站的距离
- 房龄

预测一个学生成绩时，一个样本可能包含：

- 每周学习时间
- 作业完成率
- 出勤率
- 课堂测验成绩

这时，一个样本就不再是单独一个数，而更像是“由多个特征组成的一列数”。这正是向量出现的地方。

因此，本章的目标不是一开始就进入抽象线性代数，而是先建立一种直觉：

- 向量可以看作一组有顺序的数字
- 向量可以表示一个点，也可以表示一个方向
- 向量之间可以比较远近、方向和相似性

这些思想会直接影响后面对于矩阵、梯度、距离度量、相似度和机器学习模型的理解。

## 先修知识清单

阅读本章前，建议已经初步掌握：

- 函数和图像的最基本概念
- 平面直角坐标系
- 平方与开方
- 第 2 章中关于变化和图像的基础直觉

即使这些内容还不完全熟练，也不影响本章的直觉学习。

## 直觉解释

### 1. 向量首先是一组有顺序的数字

很多学生第一次见到向量时，会以为它是很“高等”的数学对象。其实从最朴素的角度看，向量可以先理解为：

“一组按顺序排好的数字。”

例如：

```text
(2, 5)
```

可以表示：

- 一个点的坐标
- 两门课程的成绩
- 两个商品特征
- 两个传感器读数

再例如：

```text
(面积, 房龄, 楼层, 距离地铁站距离)
```

在具体数值化之后，就变成一个多维向量。

### 2. 向量既可以看作点，也可以看作箭头

例如在二维平面里，向量：

```text
(3, 2)
```

有两种非常重要的看法：

- 点的看法：它表示平面上坐标为 `(3, 2)` 的点
- 箭头的看法：它表示从原点 `(0, 0)` 指向 `(3, 2)` 的箭头

这两种看法都很重要：

- 把向量看成点，方便我们理解“样本分布在哪里”
- 把向量看成箭头，方便我们理解“方向和大小”

### 3. 向量帮助我们描述“相似”与“不相似”

假设有两个学生：

- 学生 A：`(数学 90, 英语 85)`
- 学生 B：`(数学 88, 英语 84)`

这两个向量很接近，所以我们会说他们的成绩结构比较相似。

如果另一个学生是：

- 学生 C：`(数学 40, 英语 95)`

那么它和学生 A 的差异就更大。

这说明，一旦把对象表示成向量，我们就可以讨论：

- 距离有多远
- 方向是否接近
- 哪些样本彼此更相似

这正是很多机器学习方法的基础。

## 核心概念

### 1. 向量

向量可以先理解为一个按顺序排列的数值列表。

例如：

```text
(1, 2)
```

或者：

```text
(3, 5, 7)
```

顺序非常重要，因为：

```text
(1, 2) != (2, 1)
```

在机器学习里，一个向量通常对应一条样本记录的特征表示。

### 2. 维度

向量里有几个数，就说它是几维向量。

例如：

- `(2, 5)` 是二维向量
- `(1, 3, 7)` 是三维向量
- 一个包含 100 个特征的样本，就是 100 维向量

维度越高，并不意味着概念越神秘，只是表示同时记录的特征更多。

### 3. 向量加法

两个相同维度的向量可以相加。

例如：

```text
(1, 2) + (3, 4) = (4, 6)
```

它可以理解为“对应位置分别相加”。

如果把向量看成箭头，那么向量加法也可以看成“位移的叠加”。

### 4. 数乘

向量可以和一个数字相乘。

例如：

```text
2(1, 3) = (2, 6)
```

如果把向量看作箭头，那么数乘表示：

- 方向不变或翻转
- 长度被放大或缩小

例如：

- `2v` 表示长度放大 2 倍
- `0.5v` 表示长度缩小为原来的一半
- `-v` 表示方向相反

### 5. 向量长度

向量长度也常叫范数。在二维里，向量：

```text
(x, y)
```

的长度可以写成：

```text
sqrt(x^2 + y^2)
```

例如向量 `(3, 4)` 的长度为：

```text
sqrt(3^2 + 4^2) = 5
```

长度帮助我们理解：

- 一个点离原点有多远
- 一个方向上的“强度”有多大

### 6. 距离

如果有两个点：

```text
A = (x_1, y_1), B = (x_2, y_2)
```

那么它们之间的距离可以写成：

```text
sqrt((x_2 - x_1)^2 + (y_2 - y_1)^2)
```

这就是平面中最常见的欧氏距离。

在机器学习里，很多方法都会问一个核心问题：

“哪个样本离我最近？”

一旦可以计算距离，很多分类、聚类和检索方法就有了基础。

### 7. 点积

两个向量的点积在二维里可以写成：

```text
(a, b) · (c, d) = ac + bd
```

例如：

```text
(1, 2) · (3, 4) = 1×3 + 2×4 = 11
```

点积非常重要，因为它既能做数值组合，也和方向关系有关。

直观上可以先把它理解为：

- 对应特征做加权配对
- 再把结果加总

后面在线性模型和神经网络里，你会频繁见到这种结构。

## 例题与推导

### 例 1：把一条样本记录写成向量

假设我们要描述一套房子，用三个特征：

- 面积
- 房龄
- 距离地铁站距离

某套房子的记录可能写成：

```text
(89, 12, 0.8)
```

这表示：

- 面积为 89
- 房龄为 12
- 距离地铁站 0.8 公里

这就是一个三维向量。

从这里开始，读者应逐步接受：

“数据表中的一行，在数学上常常就是一个向量。”

### 例 2：向量长度

考虑向量：

```text
v = (3, 4)
```

它的长度为：

```text
|v| = sqrt(3^2 + 4^2) = 5
```

这和勾股定理是一致的。

如果画图，你会看到这个向量从原点出发，到达点 `(3, 4)`，与坐标轴形成一个直角三角形，斜边长度就是向量长度。

### 例 3：两个样本的距离

设：

```text
A = (1, 2), B = (4, 6)
```

那么它们的距离为：

```text
sqrt((4 - 1)^2 + (6 - 2)^2)
= sqrt(3^2 + 4^2)
= 5
```

这表示点 `A` 和点 `B` 在平面中的直线距离是 5。

在机器学习里，这个距离可以用来衡量两个样本是否接近。

### 例 4：点积与加权求和

设有向量：

```text
x = (2, 3), w = (4, 5)
```

那么点积为：

```text
w · x = 4×2 + 5×3 = 23
```

可以把它理解为：

- 第一个特征乘上第一个权重
- 第二个特征乘上第二个权重
- 再把结果相加

这已经非常接近后面机器学习中常见的表达：

```text
w_1x_1 + w_2x_2 + ... + w_nx_n
```

### 例 5：为什么“方向”也重要

设两个向量：

```text
(1, 1) 和 (10, 10)
```

它们长度差很多，但方向相同。

再比较：

```text
(1, 1) 和 (1, -1)
```

这两个向量长度可能类似，但方向明显不同。

这告诉我们：两个对象是否相似，不仅和“大小差多少”有关，也和“方向是否一致”有关。

## 图示理解

先看向量作为“点”和“箭头”的双重表示：

![向量作为点和箭头](../assets/figures/ch03_point_and_arrow.png)

这张图对应本章最重要的直觉之一：

- 同一个向量 `(3, 2)` 可以看作一个点
- 也可以看作从原点出发的一支箭头
- 当我们讨论数据分布时，常把它看成点
- 当我们讨论方向和大小时，常把它看成箭头

再看两个点之间的距离：

![两点距离图示](../assets/figures/ch03_distance.png)

图中的线段把点 `A` 和点 `B` 连接起来，它表示的是两条样本记录之间的几何距离。很多机器学习方法都会依赖这种“谁离谁更近”的判断。

## Python 小实验

下面这段 Python 代码可以帮助你计算向量长度、距离和点积：

```python
from math import sqrt

v = (3, 4)
length = sqrt(v[0] ** 2 + v[1] ** 2)
print("向量长度:", length)

a = (1, 2)
b = (4, 6)
distance = sqrt((b[0] - a[0]) ** 2 + (b[1] - a[1]) ** 2)
print("两点距离:", distance)

x = (2, 3)
w = (4, 5)
dot_product = x[0] * w[0] + x[1] * w[1]
print("点积:", dot_product)
```

这段代码有三个教学作用：

- 用 `sqrt` 把勾股定理变成可执行的长度公式
- 用坐标差计算距离，理解“远近”如何量化
- 用点积看到“加权求和”的结构

如果你把 `x = (2, 3)` 和 `w = (4, 5)` 改成别的数字，就能观察不同特征组合下点积如何变化。

下面是一个更贴近机器学习表达的版本：

```python
features = (80, 12, 3)
weights = (0.8, -0.2, 1.5)
bias = 10

score = (
    features[0] * weights[0]
    + features[1] * weights[1]
    + features[2] * weights[2]
    + bias
)

print("模型输出:", score)
```

这就是后面线性模型里 `w · x + b` 的最朴素写法。

## 配图建议

本章建议至少配五张图：

1. 二维点与箭头双重解释图  
   用同一个向量 `(3, 2)` 同时展示“点”和“从原点出发的箭头”两种理解。

2. 向量加法平行四边形图  
   展示 `(1, 2) + (3, 1)` 如何得到新的向量。

3. 向量长度图  
   用 `(3, 4)` 与坐标轴构成直角三角形，帮助读者理解长度公式。

4. 两点距离图  
   标出两个样本点以及它们之间的直线距离。

5. 点积与投影直觉图  
   先用箭头展示两个向量方向的关系，帮助读者形成“方向越接近，某些乘积特征越明显”的印象。

本章图示的重点不是形式化，而是帮助学生把“数据表的一行”联想到“坐标系中的点”和“从原点出发的箭头”。

## 与机器学习的联系

### 1. 一条样本记录通常就是一个向量

机器学习中的表格数据，常常可以写成下面的形式：

```text
x = (x_1, x_2, ..., x_n)
```

这里每个 `x_i` 都对应一个特征。

因此，向量不是额外附加的数学对象，而是数据表示的自然语言。

### 2. 距离决定“谁和谁更像”

很多方法都会依赖距离概念，例如：

- 最近邻分类
- 聚类
- 相似样本检索

这些方法背后都离不开一个问题：

“两个向量之间有多近？”

### 3. 点积决定“加权组合”

在线性回归、逻辑回归和神经网络中，经常会出现：

```text
w · x + b
```

这个式子本质上就是：

- 输入向量 `x`
- 参数向量 `w`
- 做点积得到加权和
- 再加一个偏置 `b`

如果本章对向量和点积理解到位，后面看模型公式会轻松很多。

### 4. 向量空间让高维数据可被分析

虽然真实数据可能不是二维或三维，无法直接画出来，但数学思路没有变：

- 一条样本记录仍然是一个向量
- 样本之间仍然可以比较距离和方向
- 模型仍然在这些向量之间寻找规律

这就是为什么线性代数会成为机器学习数学的核心基础之一。

## 常见误区

### 误区 1：向量就是“带箭头的图形”

箭头图是帮助理解的方式之一，但向量更本质的含义是一个按顺序排列的数字对象。

### 误区 2：维度高就完全无法理解

高维只是特征更多，并不改变基本思想。二维和三维图像只是帮助我们建立直觉。

### 误区 3：距离越小就一定越相似

很多时候这是有帮助的，但还要看特征是否经过合理处理，例如量纲是否统一、特征是否标准化。

### 误区 4：点积只是一个机械计算公式

不是。点积既是数值计算工具，也是后面理解线性模型、投影和相似性的重要入口。

## 练习题

1. 判断下列向量分别是几维：`(2, 3)`、`(1, 4, 6)`、`(5, 7, 9, 2)`。
2. 计算 `(1, 2) + (3, 4)`。
3. 计算 `3(2, 1)`。
4. 求向量 `(6, 8)` 的长度。
5. 求点 `A = (1, 1)` 和 `B = (4, 5)` 之间的距离。
6. 计算向量 `(2, 3)` 与 `(4, 1)` 的点积。
7. 请你把一个现实对象写成三维或四维向量，例如“一个学生”或“一套房子”的特征表示。

## 本章小结

本章完成了从“单个数字”到“多个特征组成的对象”的过渡。

学完本章后，读者应逐步能够：

- 把向量理解为有顺序的数值列表
- 理解向量既可以表示点，也可以表示方向
- 计算并解释向量加法、数乘、长度和距离
- 理解点积是一种加权组合方式
- 接受机器学习数据中的一条样本记录通常就是一个向量

这为下一章学习矩阵，以及后续理解线性模型、相似性和高维数据分析打下了基础。
